document.getElementById("proreg").addEventListener('click', () => {
	let isValid = true;
	// const inputs = document.querySelectorAll('form > div > input');
	
	if (isValid) {
		document.forms[0].submit();
	}
});